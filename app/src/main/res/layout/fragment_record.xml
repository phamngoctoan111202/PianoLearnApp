<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/img_home_bgr"
        tools:context=".ui.RecordFragment">

        <TextView
            android:id="@+id/title_record"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Record List"
            android:layout_marginTop="35dp"
            android:textColor="@color/text_color"
            android:textSize="24sp"
            android:drawableStart="@drawable/ic_back"
            android:drawablePadding="16dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintHorizontal_bias="0.0"/>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recycler_view_record"
            android:layout_width="0dp"
            android:layout_height="0dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/title_record"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_marginTop="16dp" />

        <ImageView
            android:id="@+id/empty_record_img"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/img_record_empty"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:layout_marginTop="200dp"
            app:layout_constraintTop_toBottomOf="@+id/title_record" />

        <TextView
            android:id="@+id/empty_record_txt"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:text="There are no records"
            android:textColor="@color/text_color"
            app:layout_constraintEnd_toEndOf="@+id/empty_record_img"
            app:layout_constraintStart_toStartOf="@+id/empty_record_img"
            app:layout_constraintTop_toBottomOf="@+id/empty_record_img" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
